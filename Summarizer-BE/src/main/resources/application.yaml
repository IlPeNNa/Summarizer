spring:
  application:
    name: myapp
  
  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/summarizerdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: summarizer
    password: Tfovygv$185956
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jpa:
    hibernate:
      ddl-auto: update  # Cambia in 'validate' in produzione
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2KB

server:
  port: 8080

# Configurazione servizio NLP Python (black box)
nlp:
  service:
    url: http://localhost:8000